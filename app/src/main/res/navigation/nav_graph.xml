<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/CharacterListFragment">

    <fragment
        android:id="@+id/CharacterListFragment"
        android:name="com.nikhil.rickandmorty.ui.characters.list.CharacterListFragment"
        android:label="@string/character_list"
        tools:layout="@layout/fragment_character_list">
    </fragment>
    <fragment
        android:id="@+id/CharacterDetailsFragment"
        android:name="com.nikhil.rickandmorty.ui.characters.details.CharacterDetailsFragment"
        android:label="@string/character_details"
        tools:layout="@layout/fragment_character_details">
    </fragment>
</navigation>